- pull repository
- npm install
- create database
- create .env file i.e.:
  ```
  DATABASE_URL=postgres://<user>@localhost:5432/aiid
  ```
- Initialize the database
  `npm run init`
- Download from the AIID API
  `npm run download`